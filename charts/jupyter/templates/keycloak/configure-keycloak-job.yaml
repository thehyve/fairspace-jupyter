{{- $scripts := .Values.configurationScripts.keycloak -}}
{{- if $scripts.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{.Release.Name}}-keycloak-configuration"
  labels:
    heritage: {{.Release.Service | quote }}
    release: {{.Release.Name | quote }}
    chart: "{{.Chart.Name}}-{{.Chart.Version}}"
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: "{{.Release.Name}}-keycloak-configuration"
      labels:
        heritage: {{.Release.Service | quote }}
        release: {{.Release.Name | quote }}
        chart: "{{.Chart.Name}}-{{.Chart.Version}}"
    spec:
      restartPolicy: Never
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
      {{ toYaml . | indent 8 }}
      {{- end }}
      containers:
      - name: configure-keycloak-job
        image: "{{ $scripts.image }}"
        imagePullPolicy: {{ $scripts.pullPolicy }}
        resources:
{{ toYaml $scripts.resources | indent 10 }}
        env:
          - name: LOGIN_REALM
            value: {{ .Values.keycloak.loginRealm }}
          - name: KEYCLOAK_PASSWORD
            valueFrom:
              secretKeyRef:
                name: "{{ .Release.Name }}-keycloak-credentials"
                key: password
        volumeMounts:
        - name: config-volume
          mountPath: /opt/config
        command:
        - "/opt/jboss/scripts/add-redirect-urls.sh"
        - "{{.Values.keycloak.baseUrl}}"
        - "{{.Values.keycloak.username}}"
        - "{{.Values.keycloak.realm}}"
        - "{{.Values.jupyterhub.auth.custom.config.client_id}}"
        - "/opt/config/redirect.urls"
      volumes:
      - name: config-volume
        configMap:
          name: "{{.Release.Name}}-keycloak-config-map"
{{- end -}}
